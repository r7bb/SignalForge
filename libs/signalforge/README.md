# signalforge (core library)

Shared engine used by every SignalForge service: OCSF normalization, the Sigma
rule compiler and evaluator, the correlation/risk engine, storage adapters and
the incident state machine. See the repository root README for the full picture.
